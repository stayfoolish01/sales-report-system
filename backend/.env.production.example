# ===========================================
# 本番環境用設定ファイル (.env.production)
# ===========================================
# 重要: このファイルをコピーして .env.production を作成し、
# 実際の値を設定してください。
# .env.production はGitにコミットしないでください。
# ===========================================

# Database
# 本番環境では強力なパスワードを使用してください
# 例: DATABASE_URL="postgresql://sales_app:$(openssl rand -base64 32)@db-server:5432/sales_report_db?schema=public&sslmode=require"
DATABASE_URL="postgresql://YOUR_DB_USER:YOUR_STRONG_PASSWORD@YOUR_DB_HOST:5432/sales_report_db?schema=public&sslmode=require"

# JWT
# 重要: 必ず暗号学的に安全なランダム値を生成してください
# 生成コマンド例: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# または: openssl rand -hex 64
JWT_SECRET="GENERATE_A_SECURE_RANDOM_STRING_HERE"
JWT_EXPIRES_IN="1h"

# Server
PORT=3001
NODE_ENV="production"

# CORS
# 本番環境のフロントエンドURLを設定
ALLOWED_ORIGINS="https://your-production-domain.com"
